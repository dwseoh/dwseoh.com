{
  "name": "dwseoh-blog-stats",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for blog view/like counters on dwseoh.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
